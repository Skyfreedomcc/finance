# 💰 中小企业财务管理系统 (SME Finance System)

> **基于 Spring Boot + Vue 3 的现代化中小企业财务管理平台** > 实现“业财一体化”，让业务动作直接驱动财务核算，告别繁琐的手工记账。

## 📖 项目简介 (Introduction)

本项目是一套专为中小企业设计的财务管理系统，采用 B/S 架构，前后端分离开发。系统核心旨在解决传统手工财务核算滞后、易出错的问题。通过内置的 **“自动过账引擎”**，系统能够捕捉采购、销售、薪酬发放等业务动作，自动生成符合复式记账原则的会计凭证，并实时生成资产负债表与管理驾驶舱图表。

## ✨ 核心功能 (Key Features)

* **📊 管理驾驶舱 (Dashboard)**: 集成 ECharts，实时展示资金流向、收支趋势、待办事项及关键经营指标（KPI）。
* **🔄 业财一体化 (Auto-Posting)**:
* **采购/销售**: 业务单据审核通过后，自动关联默认科目生成凭证。
* **薪酬发放**: 一键发放工资，自动生成 `借:管理费用` / `贷:银行存款` 凭证。


* **📝 凭证管理**: 支持凭证的手工录入、审核、查询及红字冲销，严格遵循“有借必有贷，借贷必相等”原则。
* **📒 账簿与报表**:
* **序时簿**: 记录所有凭证流水。
* **科目余额表**: 自动计算期初、本期发生及期末余额。
* **资产负债表**: 动态递归计算科目树，实时展示企业财务健康状况。


* **👥 基础档案**: 客户、供应商、员工及会计科目的维护。

## 🛠️ 技术栈 (Tech Stack)

### 后端 (Backend)

* **核心框架**: Spring Boot 3
* **ORM 框架**: MyBatis-Plus (简化 CRUD 与 复杂查询)
* **数据库**: MySQL 8.0
* **工具库**: Lombok, Hutool

### 前端 (Frontend)

* **核心框架**: Vue 3 (Composition API)
* **构建工具**: Vite
* **UI 组件库**: Element Plus
* **网络请求**: Axios
* **数据可视化**: Apache ECharts
* **状态管理**: Pinia / Vuex

## 📂 项目结构 (Structure)

```text
finance/
├── finance-web/          # 前端项目 (Vue 3)
│   ├── src/
│   │   ├── api/          # 接口请求
│   │   ├── views/        # 页面组件 (Employee, Ledger, Report...)
│   │   └── components/   # 公共组件
│   └── ...
├── financeSystem/        # 后端项目 (Spring Boot)
│   ├── src/main/java/com/finance/financesystem/
│   │   ├── controller/   # 控制层 (EmployeeController, ReportController...)
│   │   ├── service/      # 业务逻辑层 (含自动过账引擎)
│   │   ├── mapper/       # 数据持久层
│   │   └── entity/       # 数据库实体
│   └── ...
└── README.md             # 项目说明文档

```

## 🚀 快速开始 (Getting Started)

### 1. 环境准备

* JDK 17+
* Node.js 16+
* MySQL 8.0
* IntelliJ IDEA / VS Code

### 2. 数据库配置

1. 创建数据库 `finance_db`。
2. 导入项目根目录下的 SQL 脚本（如 `finance.sql`），初始化表结构与基础数据。
3. 修改后端配置文件 `financeSystem/src/main/resources/application.yml`：
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/finance_db?useUnicode=true&characterEncoding=utf-8
    username: your_username
    password: your_password

```



### 3. 启动后端

进入 `financeSystem` 目录，运行主启动类 `FinanceSystemApplication.java`。

> 成功启动后，服务将运行在 `http://localhost:8080` (默认端口)。

### 4. 启动前端

进入 `finance-web` 目录，安装依赖并启动：

```bash
cd finance-web
npm install       # 安装依赖
npm run dev       # 启动开发服务器

```

> 访问终端输出的本地地址 (如 `http://localhost:5173`) 即可进入系统。


*(注：请在 `doc/images` 目录下放入你的实际截图)*



如果您发现任何 Bug 或有改进建议，欢迎提交 Issue 或 Pull Request。

## 📄 许可证 (License)

[MIT](https://opensource.org/licenses/MIT)

---

Copyright © 2025 Skyfreedomcc. All rights reserved.
